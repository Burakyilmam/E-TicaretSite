@model Entity.Entities.User
@{
    ViewData["Title"] = "Kullanıcı Kayıt Sayfası";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-5">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card">
                <div class="card-header text-center bg-dark">
                    <h3 class="text-white">Kullanıcı Kayıt Formu</h3>
                </div>
                <div class="card-body">
                    @if (!string.IsNullOrEmpty(ViewData["ErrorMessage"] as string))
                    {
                        <div class="alert alert-danger">
                            @ViewData["ErrorMessage"]
                        </div>
                    }
                    @using (Html.BeginForm("UserRegister", "User", FormMethod.Post, new { @class = "needs-validation", novalidate = "novalidate" }))
                    {
                        <div class="mt-2">
                            <div class="form-group">
                                <label for="UserName">Kullanıcı Adı</label>
                                @Html.TextBoxFor(x => x.UserName, new { @class = "form-control" })
                                @Html.ValidationMessageFor(x => x.UserName, "", new { @class = "text-danger" })
                            </div>
                            <div class="form-group">
                                <label for="Password">Parola</label>
                                @Html.PasswordFor(x => x.Password, new { @class = "form-control" })
                                @Html.ValidationMessageFor(x => x.Password, "", new { @class = "text-danger" })
                            </div>
                            <div class="form-group text-center pt-2">
                                <button type="submit" class="btn btn-success mt-3">Kayıt Ol</button>
                            </div>
                        </div>
                    }
                </div>
                <div class="card-footer text-center">
                    <h6 class="pb-2">Zaten Üye Misiniz ?</h6>
                    <a class="btn btn-danger mb-2" href="/User/UserLogin/" id="btn_login">Giriş Yap</a>
                </div>
            </div>
        </div>
    </div>
</div>
